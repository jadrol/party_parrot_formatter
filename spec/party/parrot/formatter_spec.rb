require "spec_helper"

RSpec.describe Party::Parrot::Formatter do
  it "has a version number" do
    expect(Party::Parrot::Formatter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
